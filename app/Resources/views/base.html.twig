<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>MACQ la boutik</title>

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <nav>
            <div class="hidescroll">
                <link class="navbar-logo" href="/" img src="{{ asset('images/MACQ_LOGO.png') }}" alt="" id="logo"/>
                <span></span>
                <a class="navitem" href="/"><i class="fa fa-home"></i><span>Accueil</span></a>
                <span></span>
                <a class="navitem" href="/boutique"><i class="fa fa-bars"></i><span>Boutiques</span></a>
                <span></span>
                <a class="navitem" href="/offreservice"><i class="fa fa-cloud"></i><span>Offres de services</span></a>
                <span></span>
                <a class="navitem" href="/contact"><i class="fa fa-envelope"></i><span>Contacter MACQ</span></a>
                <span></span>
                <a class="navitem" href="#"><i class="fa fa-bookmark"></i><span>Les créations</span></a>
                <span></span>
                <a class="navitem" href="apropos"><i class="fa fa-search"></i><span>A propos de MACQ</span></a>
                <span></span>
            </div>
        </nav>
    </head>
    {% block stylesheets %}
        <style>
            body {
                margin: 0;
                background: #FFFFFF;
                font-family: sans-serif;
            }
            nav {
                width: 106px;
                height: 100vh;
                box-sizing: border-box;
                padding: 20px 0;
                position: fixed;
                top: 0;
                left: 0;
                white-space: nowrap;
                overflow-X: hidden;
                overflow-Y: hidden;
                background: rgba(234,82,148);
                border-right: 12px solid rgba(252,216,54);
                transition: all 0.5s;
            }
            .hidescroll > span {
                display: block;
                padding: 1em 5em;
                font-size: 20px;
                margin: 0 0;
                color: rgba(255,255,255,0);
                font-weight: 60;
                transition: all 0.5s;
            }
            nav .navitem {
                color: rgba(0,0,0,0);
                display: block;
                position: relative;
                text-decoration: none;
                font-size: 24px;
                padding: 0.5em 2em;
                transition: all 0.1s;
            }
            nav .navitem:hover {
                color: #fff;
            }
            nav .navitem:hover i {
                color: #fff;
            }
            nav .navitem i {
                font-size: 32px;
                vertical-align: middle;
                color: rgba(255,255,255,0.5);
                padding: 8px;
                border: 4px solid;
                border-radius: 100%;
                transform: translateX(-50%);
                transition: all 0.1s 0.1s;
            }
            nav:hover {
                width: 400px;
                overflow: hidden;
            }
            nav:hover span {
                color: rgba(255,255,255,0.5);
                transition: all 0.1s 0.2s;
            }
            nav:hover .navitem {
                color: rgba(255,255,255,0.5);
            }

            .hidescroll {
                overflow-x: hidden;
                overflow-y: auto;
                max-height: 100%;
                margin-right: -100px;
                padding-right: 100px;
            }
        </style>
    {% endblock %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
